[project]
name = "langchain-agent-under-the-hood"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ravikn2017", email = "ravikn2007@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.18",
    "langchain-ollama>=1.1.0",
    "langchain-openai>=1.6.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
langchain-agent-under-the-hood = "langchain_agent_under_the_hood:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
